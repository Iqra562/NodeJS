-- To get the path of directories, we use the path core module.
--  A template engine is used to render dynamic pages.
    For this, we need to create a folder named views.
     If we do not use "views" as the folder name, we must write code to specify which folder to use for rendering the templates.
-- set method is used for configuration






































































